---
import PlaceholderImage from "./PlaceholderImage.astro";
import { splitHighlighted } from "../utils/highlightText";
import type { MethodContent } from "../types/content";

interface Props {
  content: MethodContent;
}

const { content } = Astro.props;

const titleParts = splitHighlighted(content.title, ["Method"]);
---

<section id="method" class="method">
	<div class="container">
		<h2 class="method-title">
			{titleParts.map((part) =>
				part.highlight ? (
					<span class="highlight">{part.text}</span>
				) : (
					<>{part.text}</>
				)
			)}
		</h2>

		<div class="method-stage">
			{content.items.map((item) => (
				<article class="method-card polaroid" data-hover-lift>
					<PlaceholderImage src={item.image || undefined} alt={item.title} variant="square" />
					<p class="method-card-caption">{item.title}</p>
				</article>
			))}
			<p class="method-center-text">{content.introSubtitle}</p>
		</div>
	</div>
</section>

<style>
	.method {
		background: var(--color-secondary-pale);
		padding: var(--section-padding);
		overflow: hidden;
	}

	.method-title {
		font-family: var(--font-display);
		text-transform: uppercase;
		letter-spacing: 0.01em;
		font-size: clamp(2.4rem, 5vw, 3.6rem);
		line-height: 1.05;
		text-align: center;
		color: var(--color-primary-dark);
		margin-bottom: 3rem;
	}

	.method-title .highlight {
		color: var(--color-tertiary);
	}

	.method-stage {
		position: relative;
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		grid-template-rows: repeat(2, 1fr);
		gap: clamp(1.5rem, 4vw, 2.5rem);
		max-width: 44rem;
		margin-inline: auto;
	}

	.polaroid {
		background: var(--color-white);
		padding: 0.75rem 0.75rem 2.25rem;
		box-shadow: var(--shadow-lift);
		rotate: -3deg;
	}

	.method-card:nth-child(2n) {
		rotate: 3deg;
	}

	.polaroid :global(.placeholder-image) {
		width: 100%;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		border-radius: 0;
		display: block;
	}

	.method-card-caption {
		text-align: center;
		font-weight: 700;
		font-size: 0.95rem;
		color: var(--color-primary-dark);
		margin-top: 0.75rem;
	}

	.method-center-text {
		position: absolute;
		top: 50%;
		left: 50%;
		translate: -50% -50%;
		max-width: 13rem;
		text-align: center;
		font-weight: 700;
		font-size: 1rem;
		color: var(--color-primary-dark);
		background: var(--color-secondary-pale);
		padding: 0.5rem;
	}

	@media (max-width: 800px) {
		.method-stage {
			grid-template-columns: 1fr;
			max-width: 20rem;
		}

		.method-center-text {
			position: static;
			translate: none;
			max-width: none;
			margin: 1.5rem auto 0;
		}
	}
</style>
